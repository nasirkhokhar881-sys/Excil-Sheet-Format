<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<title>Excel Search App (Urdu)</title>
<style>body{font-family:Arial;margin:20px;}input,select,button{padding:6px;margin:5px;}table{border-collapse:collapse;width:100%;}th,td{border:1px solid #ccc;padding:6px;}</style>
</head>
<body>
<h2>Excel Search App</h2>
<label>Excel File Choose کریں: </label>
<input id="fileInput" type="file" accept=".xlsx,.xls" /><br/>
<label>Sheet منتخب کریں: </label>
<select id="sheetSelect"></select><br/>
<label>Search کریں: </label>
<input id="searchInput" type="search" placeholder="Keyword لکھیں" />
<button id="clearBtn">Clear</button>
<div id="results"></div>
<script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js"></script>
<script>
const fileInput=document.getElementById('fileInput');
const sheetSelect=document.getElementById('sheetSelect');
const searchInput=document.getElementById('searchInput');
const results=document.getElementById('results');
let workbook=null,currentData=[];
fileInput.addEventListener('change',e=>{
  const f=e.target.files[0];
  const reader=new FileReader();
  reader.onload=ev=>{
    workbook=XLSX.read(new Uint8Array(ev.target.result),{type:'array'});
    sheetSelect.innerHTML='';
    workbook.SheetNames.forEach(name=>{const opt=document.createElement('option');opt.value=name;opt.textContent=name;sheetSelect.appendChild(opt);});
    loadSheet(sheetSelect.value);
  };
  reader.readAsArrayBuffer(f);
});
sheetSelect.addEventListener('change',()=>{loadSheet(sheetSelect.value);});
searchInput.addEventListener('input',()=>{renderResults(searchInput.value);});
document.getElementById('clearBtn').addEventListener('click